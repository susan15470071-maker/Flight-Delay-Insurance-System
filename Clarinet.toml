[project]
name = 'Flight-Delay-Insurance-System'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.airline-integration]
path = 'contracts/airline-integration.clar'
clarity_version = 3
epoch = 'latest'

[contracts.flight-data-oracle]
path = 'contracts/flight-data-oracle.clar'
clarity_version = 3
epoch = '3.2'

[contracts.instant-payout-processor]
path = 'contracts/instant-payout-processor.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
